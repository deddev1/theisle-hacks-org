---
import { externalResources } from '../data/external-resources';
import type { LocaleCode } from '../data/i18n/locales';
import { getT } from '../i18n/server';

interface Props {
	locale: LocaleCode;
	class?: string;
}

const { locale, class: className } = Astro.props;
const t = getT(locale);
---

<section
	class:list={['ext-resources', className]}
	aria-labelledby="ext-resources-title"
>
	<h2 id="ext-resources-title">{t('externalResources.title')}</h2>
	<p class="ext-resources__lede">{t('externalResources.lede')}</p>
	<ul class="ext-resources__list">
		{
			externalResources.map((item) => (
				<li>
					<a href={item.href} target="_blank" rel="noopener noreferrer">
						<span class="ext-resources__label">{item.label}</span>
						{item.note ? <span class="ext-resources__note">{item.note}</span> : null}
					</a>
				</li>
			))
		}
	</ul>
</section>

<style>
	.ext-resources {
		margin-top: 2rem;
		padding: 1.25rem 1.35rem;
		border: 1px solid var(--line);
		border-radius: var(--radius-md, 0.75rem);
		background: color-mix(in srgb, var(--bg-panel) 88%, transparent);
	}

	.ext-resources h2 {
		margin: 0 0 0.35rem;
		font-size: 1.15rem;
	}

	.ext-resources__lede {
		margin: 0 0 1rem;
		color: var(--ink-muted);
		font-size: 0.92rem;
		line-height: 1.55;
	}

	.ext-resources__list {
		display: grid;
		gap: 0.55rem;
		margin: 0;
		padding: 0;
		list-style: none;
	}

	.ext-resources__list a {
		display: grid;
		gap: 0.15rem;
		padding: 0.75rem 0.9rem;
		border: 1px solid var(--line-soft);
		border-radius: var(--radius-sm, 0.5rem);
		background: var(--bg-elevated);
		color: var(--ink);
		text-decoration: none;
		transition:
			border-color 0.2s ease,
			color 0.2s ease;
	}

	.ext-resources__list a:hover {
		border-color: color-mix(in srgb, var(--magenta) 35%, var(--line));
		color: var(--magenta-soft);
	}

	.ext-resources__label {
		font-size: 0.9rem;
		font-weight: 700;
	}

	.ext-resources__note {
		font-size: 0.8rem;
		color: var(--ink-muted);
		line-height: 1.45;
	}
</style>
